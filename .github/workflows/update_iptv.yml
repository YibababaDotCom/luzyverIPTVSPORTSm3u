name: Update IPTV Links

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run processing script
        run: |
cat << 'EOF' > process.py
          import requests
          import re

          source_url = "https://raw.githubusercontent.com/luzyver/IPTV/refs/heads/main/SPORTS.m3u"
          output_file = "iptv_links.txt"
          player_prefix = "https://yibababa.com/player/tcplayer/?url="

          def check_link(url):
              try:
                  # 添加必要的 headers，模拟真实浏览器访问
                  headers = {
                      'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/148.0.0.0 Safari/537.36',
                      'Referer': 'https://cdnlivetv.tv/'
                  }
                  response = requests.head(url, timeout=10, allow_redirects=True, headers=headers)
                  return response.status_code == 200
              except:
                  return False

          try:
              response = requests.get(source_url, timeout=15)
              content = response.text
              
              # 新的正则表达式：
              # 1. 匹配 #EXTINF 行，获取逗号后的名称
              # 2. 通过 (?:#.*?\n)* 跳过中间所有以 # 开头的行（EXTVLCOPT等）
              # 3. 匹配下一行以 http 开头的链接地址
              pattern = r'#EXTINF:-1.*?,(.*?)\n(?:#.*?\n)*(https?://\S+)'
              items = re.findall(pattern, content)
              
              print(f"匹配到 {len(items)} 个频道候选")
              
              valid_count = 0
              with open(output_file, 'w', encoding='utf-8') as f:
                  for name, link in items:
                      if check_link(link.strip()):
                          f.write(f"{name.strip()},{player_prefix}{link.strip()}\n")
                          valid_count += 1
              print(f"成功写入 {valid_count} 个有效链接")
          except Exception as e:
              print(f"错误: {e}")
          EOF
          python process.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add iptv_links.txt
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update IPTV links" && git push)
